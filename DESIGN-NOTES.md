# 변환 노트 — Kraken 디자인 시스템 적용

> **v7 변경 (최신)**: 라인(호기) 실측 데이터 주입 · 라인 필터 · 라인별 불량률 · 재검 자동 완료 ·
> 전체 재검사. 「18. v7 변경」 참조.
>
> **v6 변경**: 테마 토글을 최하단 해/달 아이콘으로 이동, 화면 문구 간소화. 「17. v6 변경」 참조.
>
> **v5 변경**: 공정 모션을 대시보드에서 직접 켜고 끌 수 있게 했습니다. 「16. v5 변경」 참조.
>
> **v4 변경**: ① 공정 애니메이션을 정지·이송 모션으로 재작성 ② **제품군/라인 용어 정정**
> ③ 판정 스트림에 라인(호기) 범주 추가. 「15. v4 변경」 참조.
>
> v3: ① 라이트/다크 테마 ② 정보량 축소 (6탭 → 4탭) ③ 공정 흐름 애니메이션. 「14. v3 변경」 참조.
> 1~13은 v2 시점의 적용 내역입니다.

원본: 다크 테마 · 단일 스크롤 대시보드
변환본: 라이트 테마 · 6탭 대시보드 (`index.html` + `app.js`)
원본 보관: `index_legacy_backup.html`

---

## 1. 파일 구조 변경

| 파일 | 변경 |
|---|---|
| `index.html` | 재작성 — 토큰(`:root`) · CSS · 탭 마크업만 |
| `app.js` | **신규** — 시뮬레이션 · 차트 · 렌더러 분리 (원본은 HTML 안에 인라인) |
| `index_legacy_backup.html` | 원본 그대로 보관 |
| `data.js` `img/` `server/` `n8n/` | **변경 없음** — FastAPI·n8n 연동 계약 그대로 유지 |

`/ingest` 요청 바디와 `/health` 응답 처리는 원본과 동일하므로 기존 n8n 워크플로우를 그대로 씁니다.

---

## 2. 탭 분할 (스크롤 제거)

| 탭 | 내용 | 원본 위치 |
|---|---|---|
| 01 개요 | KPI 4개 · 불량률 추이 · 등급 구성 · 제품군별 불량률 · 라우팅 | ①  + 라우팅 배너 |
| 02 판정 스트림 | 스트림 테이블(페이지네이션·CSV) · 판정 근거 드릴다운 | ② 좌측 |
| 03 정밀검사 큐 | 큐 KPI 4개 · 재검 테이블 · 알림 로그 | ② 우측 |
| 04 공정 변수 | A 핵심센서 3종 · TO 단일 지배변수 | ④ |
| 05 모델 성능 | 평가셋 F1 · 운영 검출률 · 혼동행렬 | ③ |
| 06 데이터 신뢰성 | 결측 · 드리프트 · 임계값 다이얼 · 데이터 계보 | ⑤ |

- 사이드바 240px(접힘 64px)에 **네비게이션 + 전역 필터만** (지침 F).
- 컨텍스트 바 sticky: 제목 · 데이터 기준시각 · 적용 조건 · 출처 · model/data 버전.
- 각 탭은 3스크롤 규칙 안에 들어옵니다. 1024px 미만에서는 상단 가로 탭 스트립으로 전환되고 필터도 유지됩니다.
- 활성 탭만 렌더합니다(`renderActive`) — 매 tick마다 6탭을 전부 그리지 않습니다. 렌더는 250ms로 스로틀.

---

## 3. 지침 A — 그대로 유지한 것

- Kraken Purple `#7132f5` 유일 강조색
- 흰 배경 · Near Black `#101114` · 쿨 블루그레이 중립
- 버튼 radius 12px, pill 없음
- 정의된 퍼플 스케일 밖 퍼플 없음
- Success green `#149e61` / 뱃지 텍스트 `#026b3f`

## 4. 지침 B — 버린 것

전용 폰트 → IBM Plex Sans 단일화 / 디스플레이 스케일 제거(최상위 22px) / 7px Micro 제거(최소 11px) / 간격 7단계 / radius 3종+원형 / **카드 그림자 → 1px border**.

---

## 5. 원본에서 제거한 금지 항목 (지침 I)

| 제거 | 대체 |
|---|---|
| 반원 SVG 게이지 (불량률) | 목표선 있는 추이 라인 + 스파크라인 |
| 이모지 아이콘 🔴🟠🟢📦🔀🎯📈⚠️✅ | 상태 점(6px) + 텍스트 라벨 |
| 다크/라이트 테마 토글 | 흰 배경 단일화 (토큰 이원화 방지) |
| pill 뱃지 (`border-radius:999px`) | `--r-badge` 6px |
| 카드 그림자 `0 8px 24px` | `1px solid var(--border)` |
| 행 진입 flash 애니메이션 | 제거 (`prefers-reduced-motion` 존중) |

`--r-round`는 상태 점과 ⓘ 글리프에만 남겼습니다.

---

## 6. 색 배정

**등급 = 시맨틱** (범주형 팔레트를 침범하지 않기 위해)

| 등급 | 토큰 | 이유 |
|---|---|---|
| 0 불량·저 | `--bad` | 규격 하한 이탈 |
| 1 정상 | `--good` | 유일한 양품 |
| 2 불량·고 | `--warn` | 규격 상한 이탈 — 불량이지만 방향이 반대 |

**제품군 = 범주형**: A `--c1`(브랜드=주력 계열) · TO `--c2`.
**순차 램프 `--seq-*`**: 단일 지표의 크기를 인코딩하는 곳에만 — 혼동행렬 농도, 신뢰도 바, 기여도 바, 센서 스파크라인, 진행 바.

### 퍼플 예산 (화면당 강조 1곳 + 주력 계열 1개)

개요 탭에서 퍼플은 ① 활성 탭 인디케이터·② 불량률 추이 라인(= 화면의 헤드라인 지표 = 강조 1곳)·③ A 제품군 계열(주력 1개)에만 나타납니다. 필터 칩·카드 헤더·아이콘에는 쓰지 않았습니다. 불량률이 기준선을 넘으면 추이 라인은 `--bad`로 바뀝니다.

---

## 7. KPI 재구성 (5개 → 4개)

원본의 "실시간 등급 분포"와 "제품군 자동 분류"는 단일 값이 없어 KPI 카드 조건(라벨·값·비교·맥락 4요소)을 만족하지 못합니다. → **차트로 강등**하고 KPI는 4개로 줄였습니다.

| KPI | 값 | 비교 기준 | 맥락 | 방향 |
|---|---|---|---|---|
| 불량률 | % | 학습 기준 29.5% | 스파크라인 | **하락 = good** |
| 불량 검출률 | % | 학습 A 0.69 / TO 0.56 | 스파크라인 | 상승 = good |
| 재검 처리율 | % | 목표 100% | 목표선 진행바 | 상승 = good |
| 판정 처리량 | 건/h | 설계 정격 40건/h | 정격 마커 | — |

4개 모두 ⓘ 정의 툴팁을 갖습니다(키보드 포커스로도 열림).

### ⚠ "vs 전주"를 쓰지 않은 이유

지침 E는 `vs 전주` 명시를 요구하지만, 이 대시보드는 **주 단위 축이 없는 데이터셋 재생**입니다. 없는 주간 비교를 만들어내면 지침 H(라벨 없는 수치 금지)와 충돌합니다. 대신 `data.js`의 `class_counts`에서 실제 산출한 **학습 기준선**을 비교 대상으로 쓰고, 각 KPI에 기준이 무엇인지 라벨로 붙였습니다.

- 전체 268/908 = **29.5%** · A 166/316 = **52.5%** · TO 102/592 = **17.2%**
- 원본 코드의 `target=35` 하드코딩과 `kTarget` 초기값 `15.0%` 불일치도 함께 해소했습니다.

---

## 8. 차트 (지침 E)

- 격자는 수평선 `--grid` 1px만. 세로선·테두리·배경 없음.
- **제목은 결론문이며 데이터에 따라 바뀝니다**: `"불량률이 구간 시작 대비 12.4%p 감소"`, `"A 제품군 불량률 52.1% — TO보다 36.5%p 높음"`.
- 모든 차트에 **단위 · n · 기준선 값**을 부제로 표기.
- 계열 3개 이하 → 범례 대신 선 끝 직접 라벨.
- 시간축이 아닌 차트는 값 내림차순(제품군별 불량률). 등급 구성은 서수 척도라 0·1·2 순서를 유지하고, 대신 100% 누적 막대로 정렬 문제를 없앴습니다.
- **텍스트가 있는 SVG는 컨테이너 실측 폭으로 렌더**합니다(`hostWidth`). `viewBox` 고정 + `width:100%`로 두면 375px에서 축 라벨이 6px까지 줄어들어 판독 불가가 됩니다.

---

## 9. 테이블 (지침 E)

헤더 `--surface-sunken` + 하단 1px `--border-strong` + sticky · 행 구분 1px(얼룩말 없음) · hover `--surface-hover` · 선택 `--brand-wash` + 좌측 2px 인디케이터 · 숫자 우측정렬 mono · 상태 칩 중앙 · **조건부 서식은 신뢰도 컬럼에만** · 20행 초과 → 15행 페이지네이션 + CSV 내보내기(BOM 포함 UTF-8).

시맨틱 `<table>`을 유지했고, 1024px 미만에서는 `data-h` 속성으로 카드형으로 전환됩니다.

---

## 10. 상태 처리 6종 (지침 G) — 전부 구현

| 상태 | 트리거 | 구현 |
|---|---|---|
| 로딩 | 부팅 | `--surface-sunken` 스켈레톤. 스피너 없음. 400건 워밍업을 50건 청크로 쪼개 UI 블로킹 방지, 120건에서 KPI 먼저 렌더 |
| 빈 데이터 | 판정 0건 | 원인 + `재생 시작` 버튼 |
| 에러 | `/ingest` 3회 연속 실패 | `--bad-bg` 배너 + 영향 범위 + `재연결` 버튼. 사과문 없음 |
| 오래된 데이터 | 재생 일시정지 / API 연결 유실 | `--warn-bg` 배너 + **마지막 성공 시각** 명시 |
| 필터 0건 | 제품군·등급 조합 | 필터 유지 + `조건 초기화` |
| 권한 없음 | 원시 센서 로그 카드 | 카드 자리 유지(점선), 사유 표시, 레이아웃 붕괴 없음 |

접근성: 포커스 링 항상 표시, `prefers-reduced-motion` 존중, 탭은 방향키/Home/End 지원, 흑백 변환 후에도 ▲▼·상태 점·텍스트 라벨로 정보 전달, skip link.

---

## 11. ML·분석 지표 라벨링 (지침 H)

원본에서 라벨이 없거나 모호했던 수치를 전부 출처와 함께 표기했습니다.

- `평가셋 macro-F1` / `등급별 F1 · 평가셋` (학습 시점 고정값) vs `불량 검출률 · 운영` (재생 중 누적) — 스코프를 라벨로 분리
- 정확도는 **"불균형 착시라 판단 근거로 쓰지 않음"**을 병기하고 배너로도 경고
- 예측값에 불확실성 동반: `예측 Y_Quality 0.5312 ± 0.0049 (KFold RMSE)`
- 누출 관련: `누출 제거 후 재학습 기준값 · 검출률 0.630 → 0.690`
- 피처 중요도: `모델 내장 gain importance (학습셋)`
- **드릴다운 기여도: `gain importance × |z|` 근사값 — SHAP 아님, 순위 참고용**임을 명시 (원본은 근거 없이 "기여도"로만 표기)
- 06 탭에 **데이터 계보** 카드 신설 — 9개 항목의 산출 방식 전체 목록
- 헤드라인 수치는 하나. 같은 지표가 다른 스코프로 나올 때는 라벨로 구분(`불량 검출률 · 전체` vs `불량 검출률 · A 운영`)
- 푸터·컨텍스트 바에 `model A XGBoost v3 · TO CatBoost v3` / `data 2026-07-20`

---

## 12. 타이포그래피 (지침 D)

`IBM Plex Sans 400/500/600/700` + `IBM Plex Mono 400/500`.

**IBM Plex Sans에는 한글 글리프가 없습니다.** 폰트 아이덴티티를 유지하기 위해 `IBM Plex Sans KR`을 같은 링크로 함께 로드했습니다(폴백 `Malgun Gothic`).

모든 수치 요소에 `font-variant-numeric: tabular-nums`. 테이블 숫자는 Mono + 우측정렬. 자리수는 컬럼 단위로 고정(불량률 1자리 · F1 3자리 · Y_Quality 4자리 · z 2자리 · 센서값 2자리 · 신뢰도 0자리). 큰 수는 천단위 구분 + `만` 축약으로 화면 전체 통일.

---

## 13. 검증

Chromium 실측 렌더로 확인했습니다.

- 6개 탭 전부 · 375px · 1280px · 1440px
- 흑백 변환(`grayscale(1)`) 후 정보 전달 유지
- 로딩 스켈레톤 / 필터 0건 / 오래된 데이터 배너 / 전송 실패 배너 / 권한 없음 카드 / 빈 큐
- 행 선택 → 드릴다운, 페이지네이션, 사이드바 접기, 키보드 포커스 링
- JS 에러 0건 · **하드코딩 색상 0개**(CSS `:root` 밖 및 JS 전수 검사) · 이모지 0개

샌드박스 검증 중 뜬 두 에러는 정상입니다: Google Fonts 403(외부 네트워크 차단 — 실사용 환경에서는 로드됨), `127.0.0.1:8000` CONNECTION_REFUSED(FastAPI 미실행 = SIM 모드).

### 남은 한계

- 결측·이상치는 실데이터에 포함돼 검증했지만(A 제품군 센서 절반이 상시 결측), **긴 문자열 케이스는 데이터셋의 제품 ID가 모두 `SAMPLE_###` 고정 길이라 실검증하지 못했습니다.** 실제 로트 ID가 길어질 경우 스트림 테이블의 제품 ID 컬럼에 `text-overflow` 처리가 필요할 수 있습니다.
- 드리프트 z값은 재생 데이터가 학습 데이터와 동일 분포라 항상 0 근처입니다. 실제 드리프트 상황은 재현되지 않습니다(구조는 동작).
- 차트는 외부 라이브러리 없이 토큰만 쓰는 자체 SVG 헬퍼로 구현했습니다. Plotly/Chart.js로 교체할 경우 `--c1..c6` `--seq-1..6` `--grid` `--axis`를 주입한 커스텀 테마 1개를 정의해 전 차트에 재사용하세요.


---
---

# 14. v3 변경 — 테마 · 정보 다이어트 · 공정 라인

## 14.1 라이트 / 다크 테마

사이드바 탭 바로 아래 세그먼트 컨트롤(`밝게` / `어둡게`)로 전환합니다.

- **구현 방식**: 토큰 이름을 그대로 두고 값만 재배정합니다. `:root[data-theme="dark"]` 블록 하나가
  전부이며, 컴포넌트 CSS와 JS는 한 줄도 분기하지 않습니다. 하드코딩 색상 0개 유지.
- **첫 진입값**: `localStorage` → 없으면 OS의 `prefers-color-scheme` → 없으면 라이트.
  선택은 브라우저에 저장됩니다.
- **다크 팔레트 설계**:

| 토큰 | 라이트 | 다크 | 근거 |
|---|---|---|---|
| `--brand` | `#7132f5` | `#a982fb` | 어두운 면 대비 확보. **`--c6` 값이라 정의된 퍼플 스케일 안** |
| `--brand-deep` | `#5b1ecf` | `#8f5cf7` | `--seq-4` |
| `--surface-sunken` | `#f7f7fa` | `#101114` | 브랜드 Near Black을 캔버스로 재사용 |
| `--surface` | `#ffffff` | `#1a1b20` | 카드가 캔버스보다 밝게 |
| `--seq-1..6` | 밝음→어두움 | 어두움→밝음 | 순차 램프 방향 반전 |
| semantic | 원본값 | 명도 상향 | `#149e61`→`#34c98a` 등, 색상(hue) 유지 |

`--on-brand` 토큰을 신설해 브랜드 면 위 텍스트(버튼·로고)가 두 테마에서 각각 반전되게 했습니다.

> **지침 A와의 충돌**: 지침은 "흰 배경 + Near Black 텍스트"를 유지 항목으로 못박고 있어, v2에서는
> 원본의 테마 토글을 제거했습니다. v3는 **사용자 요청에 따라 되살린 것**이며, 지침을 어기는 대신
> 토큰 계층에서만 해결해 나머지 규칙(퍼플 스케일·radius·간격·타이포)은 전부 지켰습니다.
> 다크 모드를 다시 없애야 하면 `[data-theme="dark"]` 블록과 `segTheme` 마크업만 지우면 됩니다.

## 14.2 정보 다이어트 (6탭 → 4탭)

| v3 탭 | 흡수한 v2 화면 |
|---|---|
| 01 현황 | 개요 (KPI 4→3) + 제품군별 불량률 + 라우팅 |
| 02 판정 스트림 | 판정 스트림 (컬럼 7→5) |
| 03 정밀검사 | 정밀검사 큐 (KPI카드 4개 → 요약 스트립 1줄) + 알림 |
| 04 진단 | 공정 변수 + 모델 성능 + 데이터 신뢰성 |

### 잘라낸 것과 이유

| 제거 | 이유 |
|---|---|
| KPI `판정 처리량` | 공정시간 90초 고정이라 **항상 40건/h**. 정보량 0 |
| 카드 `등급 구성 100% 누적 막대` | 불량률 KPI와 같은 정보의 재표현 |
| 카드 `제품군별 불량률` | **공정 라인 애니메이션 안으로 흡수** (각 라인에 불량률·기준선·n 표기) |
| 카드 `제품 라우팅` 정적 도식 | 애니메이션이 같은 내용을 실시간으로 보여줌 |
| 정밀검사 KPI 카드 4개 | 요약 스트립 한 줄로 압축 (대기/검사중/완료/최고령) |
| **혼동행렬 3×3 두 개** | 운영자에게 필요한 건 셀 9개가 아니라 **`놓친 불량` / `과검` 두 숫자**. 그 둘만 남김 |
| TO `S15` 대형 위젯 + 보조 중요도 막대 | 센서 4행 통일 목록으로 흡수, 중요도는 각 행 라벨에 % 표기 |
| 카드 `데이터 계보` | `<details>` 접힘으로 이동 (지침 H 라벨링은 유지, 화면은 안 먹음) |
| 카드 `원시 센서 로그 (권한 없음)` | **지침 G의 「권한 없음」 상태 구현이 목적이던 자리표시자.** 실제 기능이 없어 노이즈로 판단해 제거 |
| 등급 필터 (`불량만`/`정상만`) | 제품군 필터만 남김. 불량만 보려면 03 정밀검사 탭이 이미 그 역할 |
| 스트림 컬럼 `예측 Y_Quality`·`결측 센서` | 모델 내부값이라 표에서 빼고 **드릴다운으로 이동** |

**남은 KPI 3개** (지침 E 권장치): 불량률 · 불량 검출률 · 재검 대기. 셋 다 정의 툴팁 + 비교 기준 +
맥락(스파크라인)을 유지합니다.

> **지침 J 체크리스트에서 빠진 항목**: 「G섹션 6개 상태 전부 구현」 중 **권한 없음**만 제거됐습니다.
> 나머지 5개(로딩·빈 데이터·에러·오래된 데이터·필터 0건)는 그대로입니다. 권한 상태를 다시 살리려면
> 실제 권한 개념이 붙는 지점(예: CSV 내보내기, 원시 로그 열람)에 붙이는 편이 자리표시자보다 낫습니다.

각 탭 스크롤 높이 (1440×1000 실측): 현황 927px · 스트림 619px · 정밀검사 683px · 진단 1570px.
진단 탭만 1.5화면이고 나머지는 한 화면에 들어옵니다.

## 14.3 공정 라인 애니메이션 (시그니처)

`01 현황` 탭 상단. **A 라인 / TO 라인 2개**가 나란히 흐릅니다.

```
투입 ─── 센서 계측 ───(판정)─── 등급 분기 ─────▶ 출하
                                    └──────────▶ 재검
```

- **실제 판정 데이터로 구동**합니다. 점 1개 = 실제 판정 1건이며, `tick()`이 만든 등급이
  그대로 점의 색과 진로를 결정합니다. 장식용 더미 애니메이션이 아닙니다.
- **판정 지점 전까지 점은 회색**(`--text-muted`)이고, 판정 원을 지나는 순간 등급색을 얻습니다.
  "여기서 모델이 결정한다"를 시각으로 전달합니다.
- 불량(0·2)은 `등급 분기`에서 아래 재검 라인으로 빠지고, 정상(1)만 직진해 출하됩니다.
- 각 라인 우측에 **실시간 불량률 · 학습 기준선 · n**을 표기해 `제품군별 불량률` 카드를 대체했습니다.
- 동시 재공(in-flight) 실측 10~11개 (A 4~5 / TO 6~7). 라인 체류시간 9초로 잡아 밀도를 맞췄고,
  재생 속도(1×/2×/4×)에 연동됩니다.

### 구현상 중요한 점

- **정적 구조와 실시간 라벨을 분리**했습니다. 처음엔 250ms 렌더마다 SVG를 재구성해 애니메이션이
  계속 리셋되는 버그가 있었습니다. 지금은 구조는 지오메트리 변경(탭 진입·리사이즈·사이드바 접기)
  시에만 다시 만들고, 수치 라벨은 `updateStageLabels()`가 `textContent`만 갈아끼웁니다.
- 점은 `<circle>` **엘리먼트 풀을 재사용**하며 `cx`/`cy`/`fill` 속성만 갱신합니다.
  `requestAnimationFrame`은 **현황 탭이 활성 + 재생 중일 때만** 돌고, 그 외에는 취소합니다.
- 560px 미만에서는 스테이지 라벨과 출하/재검 텍스트를 접고 라인 기호만 남깁니다(375px 실측 267px,
  최소 텍스트 12px, 가로 오버플로 없음).

### `prefers-reduced-motion` 대응

OS가 모션 최소화면 **애니메이션을 아예 돌리지 않고**, 최근 판정 6건을 각 라인의 고정 위치에
정지 표시합니다. 등급 구성·분기 정보는 그대로 읽히며 배지가 `모션 최소화 · 정지 표시`로 바뀝니다.
Playwright `reduced_motion='reduce'`로 좌표 불변을 확인했습니다.

### 3D를 쓰지 않은 이유

요청은 "애니메이션이나 3D 모델링"이었고 **2D SVG 애니메이션**을 택했습니다.

1. 지침 I가 3D를 명시적으로 금지합니다. 2D는 요청을 만족하면서 지침과 충돌하지 않습니다.
2. three.js는 CDN이 필요합니다. 이 대시보드는 사내망·오프라인에서 도는 로컬 파일이고,
   폰트조차 차단되는 환경을 이미 확인했습니다.
3. 라인 흐름·분기라는 정보는 평면 위상이라 3D로 올려도 판독성이 늘지 않고 오클루전만 생깁니다.

회전하는 설비 3D 뷰가 발표용으로 필요하시면 별도 탭으로 붙일 수 있습니다. 다만 그건 데이터
시각화가 아니라 연출이므로, 지금 라인 도식과는 분리해서 만드는 편이 낫습니다.

## 14.4 v3 검증

- 두 테마 × 4탭 전수 렌더 · **JS 에러 0건**
- 테마 토큰 실측: 라이트 `bg #f7f7fa / brand #7132f5 / color-scheme light`,
  다크 `bg #101114 / brand #a982fb / surface #1a1b20 / color-scheme dark`
- 애니메이션: 좌표 변화 확인, `fill`이 `--text-muted` → `--g0/--g1/--g2`로 전환됨을 확인
- 라벨 실시간 갱신(n=130→132)이 애니메이션을 끊지 않음
- 375px 두 테마: 최소 텍스트 12px, 가로 오버플로 없음
- 모션 최소화: 좌표 고정, 점 12개 정지 표시
- 하드코딩 색상 0개 · 이모지 0개 · 4탭 전부 빈 컨테이너 없음


---
---

# 15. v4 변경 — 모션 재작성 · 용어 정정 · 라인 범주

## 15.1 ⚠ 용어 정정 (v3의 오류)

v3에서 제품군 A·TO를 **"A 라인 / TO 라인"으로 잘못 표기**했습니다. A·TO는 제품군(제품 종류)이며
생산 라인이 아닙니다. 다음을 전부 되돌렸습니다.

- 스트림·정밀검사 표 헤더 `라인` → `제품군`
- 스테이지 라벨 `A 라인` → `A 제품군`
- 모델 성능 카드 `A 라인 · XGBoost` → `A 제품군 · XGBoost`
- KPI 스코프 `A 라인` → `A 제품군`, 알림 문구, 결측 설명문 등

애니메이션 카드 제목도 `공정 라인 현황` → **`공정 흐름 · 제품군별 판정 경로`**로 바꿨습니다.
두 갈래로 나뉘는 이유가 라인이 둘이어서가 아니라 **모델이 제품군별로 분리되어 있어서**임을
제목이 직접 말하게 했습니다.

## 15.2 라인(호기) 범주 추가

판정 스트림 · 정밀검사 표 · 드릴다운 · CSV에 `라인` 컬럼을 넣었습니다.

```
공정시각 | 제품 ID | 제품군 | 라인 | 판정 등급 | 신뢰도
19:25:30 | SAMPLE_433 |  A   |  L1  |  1 정상   |  61%
```

- 색은 범주형 팔레트 `--c3`(L1) `--c4`(L2) `--c5`(L3). 브랜드 퍼플(제품군 A)과 시맨틱 색을
  침범하지 않습니다. 칩에 색 점 + 텍스트 라벨을 함께 넣어 색만으로 구분하지 않습니다.

### ⚠ 라인은 현재 **데모 배정값**입니다

`data.js`의 행은 `{id, y, cls, v}` 네 필드뿐이고 **라인·설비·호기 필드가 없습니다**(확인 완료).
그래서 제품 ID 해시로 L1~L3에 나눈 값을 넣었습니다. 같은 제품은 항상 같은 라인으로 가지만,
**실제 라인 데이터가 아닙니다.**

세 곳에 명시했습니다: ① 스트림 카드 안내문(주의색) ② 데이터 계보 항목 ③ `app.js` 주석.

교체 지점은 **함수 하나**입니다.

```js
const LINE_DEMO = true;              // 실제 필드 연결 시 false
const LINES = ['L1', 'L2', 'L3'];
function lineOf(id) { /* ID 해시 → 데모 배정 */ }
//   → function lineOf(rec) { return rec.line; }  로 교체
```

**라인별 불량률 비교 차트는 일부러 만들지 않았습니다.** 합성된 배정이라 라인별 불량률이
의미 없는 균등 분포로 나오고, 그걸 차트로 그리면 "L2가 문제 라인"처럼 읽히는 허위 신호가
생깁니다. 실제 라인 필드가 들어오면 라인별 불량률·설비별 드리프트를 붙이는 게 자연스러운 다음
단계입니다.

> **여쭤볼 것**: 라인 필드는 원천 데이터의 어느 컬럼에서 오나요? (PRODUCT_CODE와 별개인
> 설비·호기 ID가 있는지, 아니면 로트 ID에 인코딩되어 있는지) 알려주시면 `lineOf()`를 실제
> 필드로 바꾸고 라인별 분석까지 붙이겠습니다.

## 15.3 공정 애니메이션 재작성 — 등속 활강 → 정지·이송

v3는 점이 직선 위를 일정 속도로 미끄러지는 방식이라 "공정"으로 읽히지 않았습니다.
실제 라인의 거동인 **이송 → 스테이션 정지 → 처리 → 다음 이송**을 그대로 옮겼습니다.

### 타임라인 (1× 기준, 총 7.27초)

| 구간 | 동작 | 시간 |
|---|---|---|
| 투입 | 정지(안착) | 420ms |
| → 센서 계측 | 이송 | 1150ms |
| 센서 계측 | **정지** | 820ms |
| → 모델 판정 | 이송 | 1080ms |
| 모델 판정 | **정지 · 등급 확정** | 1150ms |
| → 등급 분기 | 이송 | 880ms |
| 등급 분기 | **정지** | 520ms |
| → 출하 / 재검 | 이송·배출 | 1250ms |

체류 시간이 전체의 **40%**이고, 이송 구간은 `easeInOutCubic`으로 가·감속합니다.
실측: 프레임당 평균 48%의 물체가 정지 상태, 개별 물체 궤적에서 240ms 이상 정지 지점 3곳 확인.

### 물체와 설비

- **물체**: 점 → **기판 형태**(18×11 둥근 사각 + 상단 하이라이트). 반도체·디스플레이 공정
  센서명(센서 4계열)에 맞춘 형태입니다.
- **컨베이어**: 벨트 본체 + `stroke-dashoffset`을 굴리는 텍스처 선으로 이송감을 줍니다.
  물체가 정지해 있어도 벨트가 흐르는 게 보여 "라인이 살아 있다"가 전달됩니다.
- **스테이션 게이트**: ⊓ 형태 설비 프레임. 판정 스테이션만 제품군 색 + 두꺼운 선 + 표식점으로
  강조해 "여기서 모델이 결정한다"를 구조로 표현했습니다.
- **디버터 암**: 등급 분기점에 붉은 사선. 불량이 아래 벨트로 밀려나는 지점입니다.
- **판정 링 펄스**: 판정 체류 중 물체 주변에 링이 퍼지며 사라지고(최대 opacity 0.53),
  그 순간 회색 → 등급색으로 바뀝니다.

### 여전히 지킨 것

- 물체 1개 = **실제 판정 1건**. 장식용 더미를 추가하지 않았습니다.
- `<g>` 엘리먼트 풀 재사용, `transform`만 갱신. rAF는 현황 탭 활성 + 재생 중일 때만.
- 구조 SVG와 실시간 라벨 분리 유지 (`updateStageLabels`).
- `prefers-reduced-motion`: 애니메이션 미실행, 최근 판정 10건을 스테이션 위에 정지 표시.
- 580px 미만에서는 게이트·스테이션 라벨·배출구 텍스트를 접고 벨트와 물체만 남깁니다.

## 15.4 v4 검증

| 항목 | 결과 |
|---|---|
| 정지·이송 교대 | 정지 물체 존재 프레임 59/59 · 평균 정지 48% (이론 40%) |
| 개별 물체 궤적 | 정지 → 가·감속 이송 → 정지 패턴 확인, 체류 지점 3곳 |
| 판정 색 전환 | `--text-muted` → `--g0/--g1/--g2` 전부 관측 |
| 링 펄스 | 관측됨, 최대 opacity 0.53 |
| 벨트 텍스처 | `stroke-dashoffset` 변화 확인 |
| 동시 재공 | 7~8개 |
| SVG 텍스트 겹침 | 1440 / 1024 / 768 / 375px 전부 **겹침 0** |
| 최소 폰트 | **11px** (10px였던 compact 메타 라벨 수정) |
| 가로 오버플로 | 4개 폭 모두 없음 |
| 다크 테마 | 벨트·물체·라벨 정상, 등급색 토큰 반영 |
| 모션 최소화 | 물체 10개 좌표 고정, 배지 `모션 최소화 · 정지 표시` |
| CSV | `공정시각,제품ID,제품군,라인,판정등급,...` |
| JS 에러 | 0건 |


---
---

# 16. v5 변경 — 공정 모션 수동 제어

## 문제

`index.html`을 열었는데 공정 모션이 움직이지 않는다는 보고를 받았습니다. 재현 결과 원인은
**OS의 애니메이션 효과 설정**이었습니다.

v4까지는 `prefers-reduced-motion: reduce`를 감지하면 애니메이션을 끄고 정지 표시로만 전환했습니다.
접근성 관점에서는 맞는 동작이지만, **Windows Server / VM 이미지는 성능 때문에 애니메이션 효과가
기본으로 꺼져 있어** 정상 환경에서도 모션이 안 보이는 결과가 됩니다. 게다가 화면에는 배지가
`모션 최소화 · 정지 표시`로만 떠서, 왜 멈춘 것인지 사용자가 알 방법이 없었습니다.

| OS 애니메이션 설정 | v4 동작 |
|---|---|
| 켜짐 | 정상 이송 |
| **꺼짐** | **정지 표시 (원인 안내 없음)** |

## 해결

OS 설정은 **기본값**으로 남기고, 사용자가 대시보드에서 **명시적으로 덮을 수 있게** 했습니다.
접근성 기본값을 존중하면서도 사용자 의도가 우선되는 구조입니다.

### 1. 사이드바에 `공정 모션` 컨트롤

`자동 / 켜기 / 끄기` 세그먼트를 테마 바로 아래에 넣었습니다.

- **자동** (기본) — OS 접근성 설정을 따릅니다
- **켜기** — OS가 꺼져 있어도 이송 모션 실행
- **끄기** — OS가 켜져 있어도 정지 표시

선택은 `localStorage`에 저장되어 새로고침 후에도 유지됩니다.

### 2. 멈춘 이유를 화면에서 설명

`자동`이면서 OS 설정 때문에 멈춘 경우, 스테이지 위에 안내 배너를 띄웁니다.

> **OS 접근성 설정 때문에 공정 모션이 꺼져 있습니다**
> 시스템의 "애니메이션 효과"가 off라 정지 화면으로 표시하고 있습니다. Windows는
> 설정 → 접근성 → 시각 효과 → 애니메이션 효과에서 켤 수 있고, OS 설정을 바꾸지 않고
> 이 대시보드에서만 켜도 됩니다.  **[모션 켜기]**

버튼 한 번으로 해결되고, 배너는 사라집니다. 지침 G의 "무슨 일 + 복구 방법 + 재시도 버튼" 원칙을
그대로 적용했습니다.

### 3. 켠 직후 빈 라인이 보이지 않도록 시딩

모션을 켜면 `flight` 배열이 비어 있어 첫 물체가 도착할 때까지 몇 초간 빈 벨트가 보였습니다.
`seedFlight()`가 **최근 판정 4건**을 제품군별로 서로 다른 진행 위치에 배치해, 켠 즉시 라인이
채워진 상태로 시작합니다. (실측: 켠 직후 물체 10개)

### 4. 배지 문구 정리

`모션 최소화 · 정지 표시` → **`이송 중` / `정지 표시` / `일시정지`**. 내부 용어 대신 라인 상태를
직접 말하게 했습니다.

## v5 검증

`file://`로 열고 OS 애니메이션 OFF 상태를 재현해 전 경로를 확인했습니다.

| 시나리오 | 결과 |
|---|---|
| OS OFF · 자동 | 정지 표시 + 원인 안내 배너 표시 |
| 배너 `[모션 켜기]` 클릭 | 이송 시작 · 배너 사라짐 · 세그먼트 `켜기`로 전환 |
| 새로고침 | 설정 유지 (`localStorage = on`) |
| 사이드바 `끄기` | 정지 표시 + 짧은 안내 + 재활성 버튼 |
| OS ON · 자동 | 기존대로 이송, 배너 없음 |
| 모션설정 × 폭(1440/375) × 테마 × 4탭 | 빈 컨테이너 0 · 가로 오버플로 없음 · 최소폰트 11px · JS 에러 0 |
| 하드코딩 색상 / 이모지 | 0개 (`⚠` 기호도 `주의:` 텍스트로 교체) |


---
---

# 17. v6 변경 — 테마 토글 이동 · 문구 간소화

## 17.1 테마 토글: 사이드바 최하단 · 해/달 아이콘

- 위치를 상단(탭 아래) → **사이드바 최하단**(연동 상태 아래)으로 옮겼습니다.
- `밝게` / `어둡게` 텍스트 → **해 / 달 아이콘**. `테마` 라벨도 없앴습니다(아이콘으로 자명).
- **이모지를 쓰지 않았습니다.** 지침 I가 이모지 아이콘을 금지하므로 인라인 SVG로 직접 그렸습니다.
  해는 `circle` + 8방향 광선 `path`, 달은 초승달 `path` 하나. `stroke="currentColor"`라 선택 상태에
  따라 토큰 색(`--text-muted` → `--brand`)을 그대로 물려받습니다.
- **접힘(64px) 상태에서도 보입니다.** 다른 필터 그룹은 텍스트 세그먼트라 접힐 때 숨지만,
  테마는 아이콘이라 세로로 쌓아 계속 노출합니다.
- `aria-label`(밝은 테마 / 어두운 테마) + `title`을 넣어 아이콘만으로도 스크린리더·툴팁 대응됩니다.

### 곁들여 정리한 것

탭 번호 `01`~`04`를 **펼친 상태에서는 숨기고 접힘 상태에서만** 노출하도록 바꿨습니다.
펼쳤을 때는 라벨이 있어 번호가 장식이고, 접었을 때는 번호가 유일한 식별자이기 때문입니다.

## 17.2 문구 간소화

### 사이드바
| 이전 | 이후 |
|---|---|
| 공정 모션 | 모션 |
| 데이터 재생 | 재생 |
| 연동 상태 | 연동 |
| FastAPI 미연결 · 화면 판정만 동작합니다 | 로그 서버 미연결 |
| FastAPI는 응답하지만 n8n 워크플로우가 꺼져 있습니다 | n8n 워크플로우가 꺼져 있음 |
| 테마 / 밝게 / 어둡게 | (라벨 삭제, 아이콘만) |

### 탭 부제
| 이전 | 이후 |
|---|---|
| 라인이 지금 정상인지 | 지금 정상인가 |
| 제품 한 건씩 판정 결과와 근거 | 제품별 판정 결과와 근거 |
| 규격 이탈 제품의 재검 진행 | 재검 진행 상황 |
| 센서 · 모델 · 데이터 상태 | 센서 · 모델 · 데이터 |

`라인이 지금 정상인지`는 v4 용어 정정 때 놓친 잔재였습니다(A·TO는 제품군).

### 카드
| 이전 | 이후 |
|---|---|
| 공정 흐름 · 제품군별 판정 경로 | 공정 흐름 |
| 실시간 판정 스트림 | 판정 스트림 |
| 데이터 계보 — 이 화면의 수치가 어디서 나온 값인지 | 데이터 계보 |
| 공정 흐름 안내 3문장 | 2문장 |
| 규격 이탈(0 하한 · 2 상한) 제품입니다. 사람이 재검사한 뒤 상태를 넘깁니다. | 규격을 벗어난 제품입니다. 재검 후 상태를 넘기세요. |
| 등급별 F1 · 평가셋 (0~1, 단위 없음) | 등급별 F1 · 평가셋 |
| 결측률 · 단위 % · 최근 60건 이동평균 | 결측률 · % · 최근 60건 평균 |
| 입력 드리프트 · 단위 표준편차(σ) · 가운데가 학습 평균 | 드리프트 · σ · 가운데가 학습 평균 |

### 삭제한 것
- 뱃지 `관리한계 = 학습 평균 ±3σ`, `재학습 없이 조절` — 바로 아래 안내문과 중복
- 표 푸터 `신뢰도 컬럼에만 조건부 서식 적용` — 지침 준수 여부를 화면에 적어둔 메타 정보였음
- 푸터 `하이브리드 재생 (SIM ↔ FastAPI ↔ n8n)` — 내부 구현 용어
- 알림 로그 `(n8n 연동 시 Discord 발송)` — 도구 이름은 연동 상태 배지가 담당
- 모델 성능 카드의 `평가셋은 학습 시점 고정값 · 운영은 재생 중 누적` — 지표마다 이미
  `· 운영` `· 평가셋` 스코프 라벨이 붙어 있어 중복. **A·TO 카드에 두 번 나오던 문구**

### 지켜서 남긴 것 (지침 H)
간소화하면서도 산출 방식 표기는 유지했습니다. 짧게 바꿨을 뿐입니다.

- `기여도는 gain importance × |z| 근사 (SHAP 아님) · 순위 참고용`
- `중요도는 gain importance(학습셋)`
- `불량 검출률 · 운영` / `등급별 F1 · 평가셋` 스코프 라벨
- `정확도 0.715 — 불균형 착시라 쓰지 않음`
- 라인 데모 배정 경고
- 기준시각 · model/data 버전

### 조사 오류 수정
`정확도 0.861는` → `정확도 0.861 —`. 숫자 뒤 조사가 받침에 따라 달라지는 문제라 조사를 아예
없앴습니다.

## 17.3 v6 검증

모션설정(2) × 폭(1440/1024/375) × 테마(2) × 탭(4) = 48조합 전수 확인.

| 항목 | 결과 |
|---|---|
| 가로 오버플로 | 6개 조합 모두 없음 |
| 사이드바 세로 잘림 | 없음 (그룹이 하나 늘었지만 여유 있음) |
| 최소 폰트 | 11px |
| 테마 아이콘 렌더 | SVG 정상 (해 2요소 / 달 1요소) |
| 테마 최하단 배치 | 확인 |
| 접힘 상태 테마 | 세로 스택으로 계속 노출 |
| 탭 번호 | 펼침 `display:none` / 접힘 `block` |
| 테마 저장 | 새로고침 후 유지 |
| 빈 컨테이너 | 0 |
| 하드코딩 색상 / 이모지 | 0개 |
| JS 에러 | 0건 |


---
---

# 18. v7 변경 — 라인 실측 데이터 · 재검 자동화

## 18.1 라인(호기): 데모 배정 → 실측값

`train_식별컬럼매칭완료.csv`의 **`LINE` 컬럼**을 넣었습니다. v4에서 데모 해시로 임시 배정했던 값을
전부 대체했습니다.

- 제품 ID **908건 전수 매칭** (`data.js` ID 집합과 교집합 908/908, 누락 0)
- **라인 6개**가 제품군에 귀속됩니다 — A 제품군 4개(T01·T05 계열), TO 제품군 2개(T10 계열)
- `lines.js` 신규 파일(8KB). `PRODUCT_ID → 라인 인덱스` 맵 + 라인별 학습 기준 불량률

| 라인 | 제품군 | 학습 기준 불량률 (실제 Y_Class) |
|---|---|---|
| LINE-04 | A | 60.3% (41/68) |
| LINE-03 | A | 52.7% (48/91) |
| LINE-02 | A | 50.0% (42/84) |
| LINE-01 | A | 47.9% (35/73) |
| LINE-05 | TO | 19.2% (55/286) |
| LINE-06 | TO | 15.4% (47/306) |

원본 CSV의 `PRODUCT_CODE`는 `A_31`(316) · `T_31`(582) · `O_31`(10)이고, `data.js`의 TO(592)는
`T_31 + O_31`입니다. 라인 귀속은 이 분류를 기준으로 맞췄습니다.

### 카운터 구조를 라인 단위로 재설계

이전에는 `state.counts[제품군][등급]`이 단일 출처였습니다. 라인 필터를 넣으려면 제품군 카운터로는
쪼갤 수 없어서, **라인을 단일 출처로 내리고 제품군은 라인 합으로 유도**하도록 바꿨습니다.

```js
state.cntLine[라인][등급]   // 단일 출처
famCount(f, g)  = FAM_LINES[f].reduce(...)   // 제품군 = 라인 합
```

덕분에 라인 필터가 KPI · 불량률 추이 · 스트림 · 정밀검사 큐에 일관되게 반영됩니다.
`agg()`의 기준선도 선택된 라인들의 실제 불량 건수를 합산해 계산하므로, LINE-04만 보면
기준선이 자동으로 60.3%가 됩니다(실측 확인).

## 18.2 라인 필터

사이드바 `제품군` 아래에 `라인` 셀렉트를 추가했습니다.

- 제품군별 `optgroup`으로 묶여 있고, **제품군 필터를 바꾸면 목록이 자동으로 좁혀지고**
  선택된 라인이 그 제품군에 속하지 않으면 `전체`로 초기화됩니다.
- 6개라 세그먼트 컨트롤(최대 3~4개)로는 무리여서 네이티브 `<select>`에 토큰을 입혔습니다
  (radius 12px, 지침 C 준수).
- 라인 칩의 색 점은 **제품군 색**(A `--c1` · TO `--c2`)입니다. 6개 라인에 6색을 뿌리면 브랜드
  퍼플이 흩어지고(지침 C 위반), A 라인이 몰려 있다는 실제 패턴도 안 보입니다.

## 18.3 라인별 불량률 (현황 탭)

v4 노트에서 "실제 라인 필드가 들어오면 붙이는 게 자연스러운 다음 단계"라고 적었던 카드입니다.
합성 배정이 아니게 됐으므로 추가했습니다.

제목이 결론문으로 갱신됩니다 — 예: **`LINE-04 불량률 66.7% — 최저 LINE-06보다 52.8%p 높음`**.
값 내림차순, 세로선은 라인별 학습 기준선, 기준선을 넘으면 막대가 붉게 바뀝니다.

A 4개 라인이 TO 2개 라인보다 구조적으로 높은 게 한눈에 보입니다. 이건 데이터의 실제 성질이고,
데모 배정으로는 절대 나올 수 없던 신호입니다.

## 18.4 정밀검사: 자동 완료 + 전체 재검사

### 이전 흐름
`대기` → [검사중] 클릭 → `검사중` → [완료] 클릭 → 큐에서 사라짐 (**클릭 2회**)

### 지금 흐름
`대기` → [재검사 시작] 클릭 → `검사중` (진행률 바 + 남은 시간) → **공정시계 12분 뒤 자동 완료** →
큐에서 사라짐 (**클릭 1회**)

- 소요 시간은 **공정시계 기준 12분**이라 재생 속도(1×/2×/4×)에 함께 스케일됩니다.
  1×에서 실시간 약 11초.
- 진행 중에는 `검사중 11분`처럼 남은 시간을 표시하고 조치 열에 진행률 바를 둡니다.
- 완료 시 알림 로그에 기록을 남깁니다(추적성 유지).
- 표 푸터에 `검사중은 공정시계 12분 뒤 자동 완료`를 명시해, 항목이 조용히 사라지는 게 아니라
  예고된 동작임을 알립니다.

### 전체 재검사 버튼

카드 헤더에 Primary 버튼. 현재 필터 조건의 **대기 항목 전부**를 한 번에 검사중으로 넘깁니다.
대기 건수를 버튼 라벨에 넣고(`전체 재검사 시작 (40건)`), 대기가 없으면 `대기 없음`으로
비활성화합니다. 실측: 40건 → 클릭 → 39건 검사중 → 14초 후 32건 완료.

## 18.5 `lines.js` 누락 대비 폴백

라인이 이제 핵심 차원이라, `lines.js`만 빠지면 라인 기준 카운터가 전부 비어 **화면이 백지가 되는**
취약점이 생겼습니다. 폴백을 넣었습니다.

- 제품군을 가상 라인으로 대체해 KPI·스트림·큐가 정상 동작
- 라인 필터와 라인별 불량률 카드는 자동으로 숨김
- 원인 안내 배너 표시: `라인 정보를 읽지 못해 제품군 단위로만 표시합니다`

실측: 배너 표시 · 필터 숨김 · 카드 숨김 · KPI 3개 정상 · 스트림 12행 · JS 에러 0.

## 18.6 v7 검증

| 항목 | 결과 |
|---|---|
| 라인 값 원본 일치 | 스트림 표 5행을 CSV와 교차 검증 — **일치** |
| ID 매칭 | 908/908 |
| 라인 필터 | LINE-04 선택 시 표에 해당 라인만 |
| 제품군 연동 | TO 선택 시 옵션 2개로 축소 + 라인 필터 자동 초기화 |
| 라인 기준선 | LINE-04 필터 시 기준선 60.3% (CSV 실측값과 동일) |
| 라인별 불량률 | 6개 라인 내림차순, A 상위 · TO 하위 |
| 재검 자동 완료 | 대기 40 → 검사중 1 → 13초 후 완료 1, 큐에서 제외 |
| 전체 재검사 | 40건 일괄 → 14초 후 32건 완료 |
| CSV | `공정시각,제품ID,제품군,라인,...` 실측 라인 포함 |
| 폴백 | lines.js 제거 시 배너 + 축소 동작 |
| 모션(2) × 폭(3) × 테마(2) × 탭(4) | 빈 컨테이너 0 · 오버플로 없음 · 최소폰트 11px · JS 에러 0 |
| 하드코딩 색상 / 이모지 | 0개 |

탭 높이: 현황 1263px(라인 카드 추가) · 스트림 619px · 정밀검사 632px · 진단 1499px.

## 18.7 남은 것

원본 CSV에 **`TIMESTAMP` 컬럼**(`2022-06-13 5:14` 형식, 약 8분 간격)이 있습니다. 현재 대시보드는
공정시계를 90초/제품으로 합성해 씁니다. 재생이 행을 무작위로 뽑기 때문에 실제 타임스탬프를 그대로
쓰면 시각이 앞뒤로 튀어 단조 증가하지 않습니다. 시간 순 재생으로 바꾸면 실제 타임스탬프를 쓸 수
있고, 시간대별 불량률 같은 분석도 가능해집니다. 필요하시면 별도로 작업하겠습니다.
